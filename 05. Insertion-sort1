const insertionSort = (n, arr) => {
    let temp = arr[n-1];

    for(let i = n-1; i>0; i--){
        if(temp < arr[i-1]){
            arr[i] = arr[i-1];
            arr[i-1] = arr[i];
        }
        else {
            arr[i] = temp;
        }
        console.log(...arr)
    }
}
