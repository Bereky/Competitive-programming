const numIdenticalPairs = (nums) => {
    const n = nums.length;
    let pairs = 0;

    for(let i=0; i<n-1; i++){
        for(let j = i+1; j<n; j++){
            if(nums[i] === nums[j]){
                pairs += 1
            }
        }
    }

    return pairs
};
